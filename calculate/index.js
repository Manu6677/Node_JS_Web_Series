const {calculateSum} = require('./sum.js');

const {calculateMultiply} = require('./multiply');

const {divideTheNumbers} = require('./divide/divide.js')

module.exports = {calculateSum, calculateMultiply, divideTheNumbers}